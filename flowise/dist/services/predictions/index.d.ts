import { Request } from 'express';
import { Server } from 'socket.io';
declare const _default: {
    buildChatflow: (fullRequest: Request, ioServer: Server) => Promise<any>;
};
export default _default;
